你可把这段直接替换到日志里。

## 原代码修改明细（精确到修改点）

### 1) `preprocess/process_icews.py`
#### 修改点 A：新增 `argparse` 导入
- 原：
```python
import os
from pathlib import Path
import pickle
改：
import os
import argparse
from pathlib import Path
import pickle
修改点 B：替换主函数中写死数据集的代码
原：
if __name__ == "__main__":
    # datasets = ['ICEWS14', "ICEWS05-15",'GDELT',"ICEWS18"]
    datasets = ['ICEWS14']
    for d in datasets:
        print("Preparing dataset {}".format(d))
        prepare_dataset(os.path.join(DATA_PATH, d),d)
改：
if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Build ID-mapped files for CTpath datasets")
    parser.add_argument(
        "--datasets",
        type=str,
        default="ICEWS14",
        help="Comma-separated dataset names under ../data (e.g. ICEWS14,CHRONIC)",
    )
    args = parser.parse_args()

    datasets = [x.strip() for x in args.datasets.split(",") if x.strip()]
    for d in datasets:
        print("Preparing dataset {}".format(d))
        prepare_dataset(os.path.join(DATA_PATH, d), d)
2) preprocess/path_data.py
修改点 A：新增导入
原：
import numpy as np
import pickle
DATA_PATH = ".."
改：
import numpy as np
import pickle
import argparse
import os
DATA_PATH = ".."
修改点 B：替换主函数中写死数据集的代码，并新增目录创建
原：
if __name__ == '__main__':
    # for data_name in ["ICEWS14", "ICEWS18", "ICEWS05-15", "GDELT"]:
    for data_name in ["ICEWS14"]:
        inpath = f"{DATA_PATH}/data/{data_name}/"
        outputPath = f"{DATA_PATH}/path_data/{data_name}/"
        fileName = "train.pickle"
改：
if __name__ == '__main__':
    parser = argparse.ArgumentParser(description="Build path_data numpy files for CTpath datasets")
    parser.add_argument(
        "--datasets",
        type=str,
        default="ICEWS14",
        help="Comma-separated dataset names under ../data (e.g. ICEWS14,CHRONIC)",
    )
    args = parser.parse_args()

    for data_name in [x.strip() for x in args.datasets.split(",") if x.strip()]:
        inpath = f"{DATA_PATH}/data/{data_name}/"
        outputPath = f"{DATA_PATH}/path_data/{data_name}/"
        os.makedirs(outputPath, exist_ok=True)
        fileName = "train.pickle"
3) preprocess/init_rw.py
修改点 A：新增导入
原：
import numpy as np
import json
import time
from pathlib import Path
改：
import numpy as np
import json
import time
from pathlib import Path
import os
修改点 B：替换主函数中写死数据集的代码
原：
if __name__ == '__main__':
        # for data_name in ["ICEWS14", "ICEWS18", "ICEWS05-15", "GDELT"]:
        for data_name in ["ICEWS14"]:
改：
if __name__ == '__main__':
        parser = argparse.ArgumentParser(description="Compute MERW transition input for CTpath datasets")
        parser.add_argument(
                "--datasets",
                type=str,
                default="ICEWS14",
                help="Comma-separated dataset names under ../path_data (e.g. ICEWS14,CHRONIC)",
        )
        args = parser.parse_args()

        for data_name in [x.strip() for x in args.datasets.split(",") if x.strip()]:
修改点 C：写 edge_input 前自动建目录
原：
file = open(f"{DATA_PATH}/edge_input/{data_name}/{data_name}.in", "w")
改：
os.makedirs(f"{DATA_PATH}/edge_input/{data_name}", exist_ok=True)
file = open(f"{DATA_PATH}/edge_input/{data_name}/{data_name}.in", "w")
4) models/learner.py
修改点：load_merw 支持 CHRONIC
原：
if name in ['ICEWS14', 'ICEWS18', 'ICEWS05-15', "GDELT"]:
改：
if name in ['ICEWS14', 'ICEWS18', 'ICEWS05-15', "GDELT", "CHRONIC"]:
新增文件
preprocess/build_chronic_tkg.py

如果你要，我下一步可以再给你一个“只保留变更摘要（不
